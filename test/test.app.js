// TODO: spin up throw-away server, user HTTP client to assert (ejs) routes return HTTP200, screen-scrape if necessary

describe("[test.app.js]", function() {

    // TODO: spin up throw-away server
    // TODO: user HTTP client to hit and assert (ejs) routes return HTTP200
    // TODO: screen-scrape if necessary to verify html content, etc. etc.

});
